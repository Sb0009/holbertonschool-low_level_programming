#include <stdio.h>

/**
 * main - number of arguments passed into it.
 * @argc: counter
 * @argv: arr
 * Return: Always 0.
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
