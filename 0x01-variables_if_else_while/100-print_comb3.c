#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
int i ;
if(i = 0 ; i < 89 ; i = i ++)
{
putchar("iteration %d \n");
}
{
else("valeur de i apres la boucle : %d \n", i + 2);
putchar(' ');
putchar(,);
return (0);
}
