#include "main.h"
/**
* _islower- Main function define if c is lower or not
* Return:1 ou 0
* @c:character to be tested
*/

int  _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
